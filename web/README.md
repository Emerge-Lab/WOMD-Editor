# Waymo Scenario Studio

A Vite + React based playground for inspecting and editing Waymo Open Motion Dataset (WOMD) scenarios

## Getting started

1. Install dependencies (requires Node.js 18+):

   ```bash
   npm install
   ```

2. Start the development server:

   ```bash
   npm run dev
   ```
